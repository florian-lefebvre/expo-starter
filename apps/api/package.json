{
	"name": "@repo/api",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "wrangler types && vite",
		"build": "wrangler types && tsc && vite build",
		"preview": "vite preview"
	},
	"dependencies": {
		"@orpc/openapi": "^2.0.0-beta.17",
		"@orpc/server": "^2.0.0-beta.17",
		"@repo/router-contract": "workspace:*"
	},
	"devDependencies": {
		"@vite-deploy/cloudflare": "^0.1.0",
		"typescript": "^7.0.2",
		"vite": "^8.1.4",
		"wrangler": "^4.110.0"
	}
}
